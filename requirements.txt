# Phiên bản đã kiểm chứng chạy được trên máy hiện tại (Python 3.12.6, Windows x64).
# Ghim cứng để máy khác cài ra đúng môi trường, tránh lệch API.

# --- Bắt buộc để chạy inference ---
ultralytics==8.4.9
onnxruntime==1.24.4
opencv-python==4.13.0.92
# Máy không cần cửa sổ hiển thị có thể dùng bản nhẹ hơn thay dòng trên:
#   opencv-python-headless==4.13.0.92
# (code không gọi cv2.imshow nên cả hai đều chạy)
numpy==2.4.4
pymodbus==3.13.0
pyserial==3.5

# --- Chỉ cần khi tự export best.pt -> best.onnx ---
# (repo đã kèm best.onnx nên có thể bỏ 3 dòng này nếu muốn cài nhẹ)
torch==2.9.1
onnx==1.22.0
onnxslim==0.1.96
